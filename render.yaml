services:
  - type: web
    name: enjoyable-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python backend/simple_app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.9"